[project]
name = "f1grid"
version = "0.1.0"
description = "Leakage-free, walk-forward-evaluated Formula 1 prediction engine on real FastF1 data."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
