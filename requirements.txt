# Pinned EXACTLY. These are the versions the saved models in artifacts/models were
# trained and serialized with (the XGBoost booster JSON header records version [3,3,0],
# confirmed by loading the boosters). A deploy platform that installed newer libraries
# could load the models wrongly, change predictions, or emit XGBoost's cross-version
# "loading a pickled model from a different version" warning. Pinning these six exactly
# keeps the serving models bit-identical to the ones that made the published prediction.
xgboost==3.3.0
scikit-learn==1.9.0
numpy==2.4.6
pandas==2.3.3
joblib==1.5.3
pyarrow==24.0.0

# Not model-serialization sensitive: kept at sensible minimums.
fastf1>=3.3
scipy>=1.11
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
streamlit>=1.33
plotly>=5.20
